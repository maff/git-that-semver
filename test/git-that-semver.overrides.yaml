strategies:
  docker:
    enabled: true
  java:
    enabled: true
  python:
    enabled: true
    nightly:
      suffixTpl: "-python{% if env.PYTHON_SUFFIX %}{{ env.PYTHON_SUFFIX | prepend: '-' }}{% endif %}"
      useChangeRequestIdentifier: false

output:
  env:
    prefix: GIT_THAT_SEMVER_
